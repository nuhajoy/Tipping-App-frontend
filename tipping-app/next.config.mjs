/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      "via.placeholder.com",
      "res.cloudinary.com",
      "letsenhance.io",
      "lh3.googleusercontent.com",
      "onedrive.live.com", 
      "onedrive.live.com",
      "1drv.ms", 
    ],
  },
};

export default nextConfig;
